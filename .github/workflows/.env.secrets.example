# Secretos usados por el workflow de release de pcbox-api.
# Ver obtain-secrets.md para saber cómo conseguir cada valor.
# Cargalos como secretos del repositorio en GitHub
# (Settings > Secrets and variables > Actions > New repository secret).

DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=
INFRA_HUB_DISPATCH_TOKEN=
